using VContainer;
using VContainer.Unity;

namespace Runtime.DI
{
	public sealed class BootstrapLifetimeScope : CustomLifetimeScope
	{
		protected override void Configure(IContainerBuilder builder)
		{
		}
	}
}