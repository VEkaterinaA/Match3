using System;

namespace Runtime.Data.Constants.Strings
{
	internal static class ClassName
	{
		internal const String ButtonAnimation = nameof(ButtonAnimation);

		internal const String ManualRepaint = nameof(ManualRepaint);

		internal const String ProgressSlot = nameof(ProgressSlot);

		internal const String CloseButton = nameof(CloseButton);

		internal const String PopupPanel = nameof(PopupPanel);

		internal const String Localized = nameof(Localized);
	}
}